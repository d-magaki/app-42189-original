<!DOCTYPE html>
<html>
  <head>
    <title>App42189Original</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!--Importmap方式でJSを読み込む -->
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-light <%= 'devise-page' if devise_controller? %>">
    <header class="navbar navbar-expand-lg navbar-dark bg-dark px-4 mb-4 justify-content-between">
      <div class="d-flex align-items-center gap-3">
        <%= link_to "業務管理システム", root_path, class: "navbar-brand mb-0" %>

        <% if user_signed_in? %>
          <span class="logged-in-user text-white small">
            ログイン：<%= "管理者　" if current_user.role == "管理者" %><%= current_user.user_name %>
          </span>
        <% end %>
      </div>

      <div class="d-flex gap-2">
        <% if user_signed_in? %>
          <%= link_to "トップページ", root_path, class: "btn btn-outline-light btn-sm" %>
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-light btn-sm" %>
        <% else %>
          <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline-light btn-sm" %>
        <% end %>
      </div>
    </header>

    <main class="container">
      
      <% unless (devise_controller? && action_name == "new") || (controller_name == "home" && action_name == "index") %>
        <% if notice.present? %>
          <div class="text-center text-success fw-bold mt-3"><%= notice %></div>
        <% end %>
      <% end %>
      <% unless (devise_controller? && action_name == "new") || (controller_name == "home" && action_name == "index") %>
        <% if alert.present? %>
          <div class="text-center text-danger fw-bold mt-3"><%= alert %></div>
        <% end %>
      <% end %>

  <%= yield %>
</main>
    <!-- フッター -->
    <footer class="text-center mt-5 py-3 text-muted">
      &copy; <%= Time.current.year %> 社内業務管理システム
    </footer>
  </body>
</html>
